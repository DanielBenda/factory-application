type Worker {
    id: UUID!
    created: Date!
    createdBy: String!
    department: String!
    name: String!
    surname: String!
    systemRole: String!
    workPosition: String
}

type WorkerPage {
    items: [Worker!]!
    totalCount: Long!
}

extend type Query {
    workers(page: Int!, size: Int!): WorkerPage!
    worker(id: UUID!): Worker
}

extend type Mutation {
    createWorker(input: CreateWorkerInput!): Worker!
    updateWorker(id: UUID!, input: UpdateWorkerInput!): Worker!
    deleteWorker(id: UUID!): Boolean!
}

input CreateWorkerInput {
    department: String!
    name: String!
    surname: String!
    systemRole: String!
    workPosition: String
}

input UpdateWorkerInput {
    department: String!
    name: String!
    surname: String!
    systemRole: String!
    workPosition: String
}